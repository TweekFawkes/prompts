# Tech Stack: SQLite Database

<tech_stack>
<database type="SQLite">
Use SQLite as the database. Store the database file within the project directory (e.g., `data/app.db`).

SQLite is chosen for simplicity — it requires no separate database server and works well for single-instance deployments.
</database>
</tech_stack>
