# Tech Stack: Cloudflare R2 Object Storage

<tech_stack>
<storage type="Cloudflare R2">
Use Cloudflare R2 for file/object storage. R2 is S3-compatible, so use the standard S3 SDK or API patterns to interact with it.

Use R2 when the application needs to store or serve user-uploaded files, images, documents, or any binary data.
</storage>
</tech_stack>
